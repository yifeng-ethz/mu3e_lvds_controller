# Agent Notes

- Keep this worktree isolated from the parent `mu3e-ip-cores` repository unless
  the user explicitly asks to sync changes back.
- Prefer blunt, technically precise wording for design reviews and bug reports.
  Hit the point directly; do not soften a weak or unsafe design just to make it
  sound nicer.
